import React from 'react';
import './banner.css';

const Banner = () => (
  <div className="banner text-center">
    <p className="m-0">Click on an image to earn points, but try not to click on same image twice!</p>
  </div>
);
export default Banner;